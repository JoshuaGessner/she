---
id: PRO-001
title: Roadmap & Milestones
status: accepted
owner: process
tags: [roadmap, milestones, scope, planning, production]
updated: 2026-08-25
related: [DES-001, TEC-001, TEC-003]
---

# Roadmap & Milestones

**Philosophy:** prove the *feel* before building the *systems*. This game lives or dies on one sentence — *"I have enough. I should leave. One more room."* Everything before that sentence is provably fun is speculative work.

The most common way a project like this dies is building the meta-progression first, because it's the most fun part to design. **Do not.** The meta-layer is worthless if the run isn't good.

> **Notation (ADR-063).** Milestone scope is written as checkbox tasks with permanent IDs so `tools/status.py` can read progress straight out of this file. States: `[ ]` not started · `[~]` in progress · `[x]` done · `[-]` cut. The `→ DOC-ID` suffix at the end of a task line names the docs that task implements. Task IDs are never reused or renumbered. **This notation carries no schedule** — see *Estimates are lies* below.

---

## M0 — Design Lock
<!-- milestone id=M0 size=0 -->
**Goal:** a signed-off design document set.
**Deliverable:** these docs.

> **GATE M0 EXIT** `passed 2026-08-14` — `DES-001` through `DES-008` at status `accepted`; `OPEN-QUESTIONS.md` empty of blocking items.

> **Reordered by ADR-008.** Co-op is no longer a late milestone — it is a constraint on every milestone. Networking exists from M1.

> **DECIDED (ADR-034): solo project, no fixed timeline.** Calendar estimates are removed — on a solo project they produce guilt rather than throughput, and the **exit gates were always the real mechanism.** They are pass/fail on the game being good, never on elapsed time.
>
> Sizes below are *relative effort only*. The one piece of discipline that still matters without a deadline forcing it: **clear a milestone's exit gate before starting the next one.**

## M1 — The Feel Prototype  ·  *smallest milestone*
<!-- milestone id=M1 depends=M0 size=1.0 -->
**Goal:** answer "is moving and fighting in this space enjoyable?" — with grey boxes and zero content.
- [x] `M1-T01` First-person controller: walk, sprint, crouch, stamina, weight affecting movement. *Unjuiced per Swink; couplings measured, encumbrance signed off 2026-08-15* → DES-009, DES-005
- [x] `M1-T02` One weapon, one enemy, hit reactions, death. *Built with the awareness ladder (ADR-072); anatomy and telegraph measured. Signed off 2026-08-16 on DES-009's actual M1 criterion — swinging and connecting feel decent unjuiced. The avoidance question moves to `M2-T02`, where there is finally something worth declining (ADR-079)* → DES-009, DES-013
- [x] `M1-T03` One hand-built room set, no generation. *A cycle, not a corridor: two routes to the exit, one enemy-free, both asserted by `--route-probe`. The Prize costs -18% walk speed and triples your audible radius, so the Guardian room poses a real question. Signed off 2026-08-16* → DES-015
- [x] `M1-T04` Weight & Clamor as visible debug numbers. *Layer 1 radius, not the M2 field (ADR-073); readout plus an audible-radius ring, since TEC-001 calls this untunable blind* → DES-005
- [x] `M1-T05` **Two players over localhost**, host-authoritative (`TEC-004`). *The peer owns its body, the host owns every consequence (ADR-082) — `TEC-004` asked for prediction and banned reconciliation, which cannot both hold. A two-process smoke test runs in the pre-commit sweep* → TEC-004
- [x] `M1-T06` **Networking spike test — go/no-go:** 4 peers, ~150 synchronized entities. This validates or kills the whole Godot high-level-multiplayer approach, and it must happen now rather than at M4. **GO (ADR-068)** — CPU never the constraint; bandwidth is, and relevance filtering is load-bearing → TEC-004
- [x] `M1-T07` **Determinism harness in CI**: same seed on two processes → identical layout hash. *Built before the generator on purpose — written first it is a specification `M4-T01` must satisfy, written after it only ratifies whatever the generator already does* → TEC-001, DES-015
- [x] `M1-T08` **Godot project skeleton** — folder layout, the ≤6 autoload budget enforced in CI (ADR-066), Forward+, naming and GDScript conventions, CI for the doc index and the dashboard. *The determinism harness is `M1-T07`'s deliverable, not this one (ADR-067)* → TEC-002, TEC-001
- [x] `M1-T09` **Ink shader spike** — grey boxes, outlines and boil only. **Go/no-go**: if it is not ~70% convincing, commit to flat quantised shading and never build the other path (ADR-062, ADR-064). **GO (ADR-070)** — pass costs ≈0.4–0.6 ms at 1080p; flat quantised shading is not built → ART-005
- [x] `M1-T10` **Shared humanoid rig — seven sockets, authored to the collider** (ADR-080). `sock_head` `sock_hand_r` `sock_hand_l` `sock_back` `sock_hip_r` `sock_hip_l` `sock_shoulders`; Body and Arms are *skinned*, not socketed (ADR-057). Built to 1.80 m standing, 0.35 m radius, 1.62 m eye — the dimensions `M1-T01` was signed off against. Must exist before *any* character work; adding a socket later means re-exporting every mesh → ART-004, DES-020

> **GATE M1 EXIT** `passed 2026-08-16` — *two people who aren't you play for 10 minutes and ask to keep playing.*

**If this fails, nothing else matters.** Iterate here as long as needed. Do not proceed on hope.

## M2 — The Loop Prototype  ·  *~1.5× M1*
<!-- milestone id=M2 depends=M1 size=1.5 -->
**Goal:** prove the target sentence.
- [x] `M2-T08` **Data schema base resources** — `ItemResource` + trait resources, stable string IDs, **plus the CI validator**. Built with the first ten resources, not the first thousand. *Moved ahead of `M2-T01` by ADR-083. `WieldableTrait` only — the other six traits describe systems that do not exist yet. Validator implements the rules whose data exists and no others (ADR-084): a rule against an empty folder is a green tick that cannot fail. Item text is translation keys (ADR-084)* → TEC-006
- [x] `M2-T01` Loot with weight and clamor; **one inventory: grid-based, weighted, real-time** (ADR-040, reaffirmed by ADR-083 — decided, not a prototype fork). *6×5 grid ⟨tune⟩, `ItemInstance` + `ItemCatalogue`, host-validated pickup and drop, blockout bag screen. The room set's Prize is `glt_altar_plate` and its hand-rolled loot path is deleted. Carried clamor is a decay floor, so dropping loot buys silence back (ADR-087)* → DES-008, DES-005, DES-019
- [x] `M2-T02` The Hunt: clamor field, **the Gullsjúkr** (`DES-017` — wealth-sensing, gold-baiting, the whole point), escalation. *It navigates the clamor gradient and never a player transform (`TEC-001`), feels carried tribute through walls, and stops for gold a player disturbed — authored floor treasure is scenery to it (ADR-089). The throw verb is built. **The Sealing moved to `M2-T04`**, where the Shafts it seals exist* → DES-017, DES-005
- [x] `M2-T03` **The Ear + adaptive audio driver** (`DES-018`, ADR-035/036) — both channels together, from the first build. *One `HuntMix` computed by `AudioDirector`, rendered by the score **and** the Ear — the twin is structural, and `--ear-probe` refuses a channel nobody draws. Raw Godot, no middleware: `ART-002` chose vertical remixing, which Godot does natively (ADR-090). Score layers are blockout; `M2-T09` authors the theme* → DES-017, DES-018, DES-019, TEC-005
- **Standing test from here on: every milestone must be playable to completion with audio muted**
- [x] `M2-T04` Extraction: reach an exit, keep what you carried — **plus the Sealing** (`DES-005` Layer 3), moved here from `M2-T02` by ADR-089 because it seals the Shafts this task builds. *Two ways out: the Shaft (fixed, known, loud) and the Waystone (loot, capped at one, consumed). **The Sealing never locks the Shaft, it makes it worse** — 4.0 s to 12.8 s and 5.0 to 16.0 clamor at full escalation — because on one floor a lock is the trapping ADR-015 forbids (ADR-091). `ExtractionTrait` built. The Settle beat is absent, not faked: the loop closes and reports, the Lair is `M2-T06`* → DES-005, DES-002
- [x] `M2-T05` Death: lose it all — plus **downed state and ember rescue** (`DES-012`). *The other half of `DES-002`'s loop: `M2-T04` built "extract", this builds "or die". **The ember is an `ItemResource`**, so the sacrifice falls out of the grid, the scales and the clamor floor rather than being special-cased — 3.40 → 2.94 m/s and silent → heard at 2.2 m for whoever carries it (ADR-092). `ItemInstance` gains `bound_to`, its first mutable field, exactly as ADR-084 said it would. Vörðr **Return** and Scars are absent, not approximated — both need the LIFE `M3` builds* → DES-002, DES-003, DES-008, DES-012
- [x] `M2-T06` Minimal Lair: stash and re-descend — *and with it the **Settle beat** `M2-T04` deliberately left absent: what you brought, the keep-or-give decision made physically at the hoard (`DES-019`). **Chamber and Threshold as two scenes** (ADR-021); the Chamber has no `CoopSession` in it at all, which is what makes "never networked" structural rather than remembered. Tribute is the drag-out-of-the-bag gesture, and the place you are standing decides what it means. `GameState` holds `DES-003`'s three tiers — **the stash dies with you, the hoard never does**. The game now boots into the Threshold (ADR-095). **The re-descend half was missing and this was ticked anyway** — the stash was write-only for two milestones, because nothing ever called `GameState.withdraw()` and the doorway that documented itself as the loadout never loaded one. Closed by ADR-098, which found it through an orphaned function rather than through play* → DES-002, DES-008, DES-014, DES-019
- [x] `M2-T07` **Party scaling instrumented from the first build**: per-capita extracted value at 1/2/4 players → DES-012, TEC-004
      *Two exponents carry the whole relationship: loot **sub**-linear and clamor **super**-linear, so a bigger party is individually poorer and collectively louder with no rule saying "four players get less" (ADR-096). Per head, 1→4 players: 4.00 → 2.25 loot, ×1.00 → ×1.62 clamor. Clamor scales at `ClamorSource.add()` and nowhere else, so the Hunt meets a four-stack sooner without knowing why. `--scaling-probe` asserts the **signs**, not the ⟨tune⟩ values, at **every** size up to `MAX_PARTY` — three players lands at 2.67 loot and 1.47 clamor per head, between two and four. The co-op probe now builds its own floor rather than inheriting a scaled one — party scaling broke it in five places, none of them replication faults. **The cohesion pass then found the scaling was dead in every real session** (ADR-097): the host lays its floor in the frame it opens the session, and every other body arrives after that, so `size_of()` counted one forever. The floor is topped up as the party arrives and never shrinks, the multiplier is on what people **do** and not what they **hold**, and the smoke now checks the floor a second player is actually standing on*
- [x] `M2-T09` **Threshold theme + adaptive driver prototype** — the emotional anchor and the highest-risk audio tech, both cheap to test early → ART-002, ART-003, TEC-005
      *`ART-002`'s **three sonic worlds are three pieces**, and a level says which by calling `AudioDirector.enter()`. One flat layer table meant the Threshold and her Chamber were both scored by the Hunt — the safest and the strangest rooms in the game playing music that says how much trouble you are in (ADR-099). The camp is **warm on arrival** and its arrangement fills out as the camp does (ADR-050), driven by descents because descents are the only camp state that exists yet. **The reserved instrument is enforced**: the Hunter's voice may appear once anywhere in the game, and `--threshold-probe` fails if a second ever does — a warning that is sometimes false is worse than none. `--ear-probe` now asserts **silence is the default** in both directions, and corrected its own first draft: the rule is that *your* silence gets quiet, not that the world does. Blockout notes, real architecture — one key, one phrase length, every layer valid against every other, which is what `ART-003` says cannot be retrofitted*

- [x] `M2-T10` **Playable shell**: main menu, settings, pause and the way back out, join codes, and the diegetic blockout layer — *added by ADR-100 because both `M2` gates are playtest gates and neither could be run: no front door, no way to leave a level but killing the process, no way to change a volume, no way for two machines to meet, and no sound for anything the world does. **A join code is a shorter way to write an address, not matchmaking** — no relay, no hole-punching, and the host screen says so; `M4-T07` replaces where the address comes from and nothing else. Also fixed two systems that were built, correct and **never shown**: the Ear was never instantiated (so `DES-018`'s muted-playable rule was false while every check passed) and `_reaching_for` was never drawn* → DES-018, DES-019, TEC-004, ART-002
- [x] `M2-T11` **The pass before testing** — *ADR-101, and it found the one that mattered: **every doorway in the game was a disconnect.** The peer outlives the level, so walking from the Threshold into the Deep built a second session on a live connection and called `create_server` on a port it already held — with the whole sweep green, because `run_coop.py` never changes scene and no single-process probe has a peer to lose. `run_doorway.py` walks two processes through a door. Also: **the party descends together** (the hole is the host's decision), a failed join **returns to the menu with a reason instead of quitting the game** — and Godot's own `connection_failed` never fired against a dead port at all, so there is an 8 s ⟨tune⟩ deadline of our own — plus who-is-connected and the control list at the fire* → TEC-004, DES-012
- [x] `M2-T12` **The first remote test, and the private door** — *ADR-102. Two players connected over the internet and then found five faults with one shape: **every one was a per-player transition.** `M2-T11` fixed the door the party walks through together and nothing had looked at the door exactly one player walks through. A client could not extract (the handler ran host-side on whichever body reached the Shaft, so the **host** went to the hoard room holding the **client's** loot); a dying client was never told and lay on the floor; the Chamber gave anyone but the host a **black screen** (authority defaulted to peer 1, and that room has no camera of its own); returning from it left a client with **no body at all**; and remote movement was never interpolated — **still on 70% of frames**. Now: the door **despawns** you rather than hiding you, the room is an overlay with **its own peerless multiplayer** so nothing in it can reach the wire, seats are keyed to the peer so a door is not an identity change, and remote bodies are eased — **7%**. `run_doorway.py` walks a client through a private door; all four rows fail on the previous commit by name* → TEC-004, DES-012, DES-014

- [x] `M2-T13` **You can see where to go** — the lighting language, the landmarks, and the two x-rays that were corrupting the gate. *ADR-105. `ART-001` says lighting design **is** gameplay design and cannot be handed off as polish; `PRO-001` had handed all of it to `M4-T05` and no task anywhere mentioned wayfinding, so the floor ran on one directional sun and flat 0.85 ambient — six identically-lit boxes, and a way out that was a pale disc on the floor of one of them. `--route-probe` passed throughout: it asserts a clean route **exists**, never that anyone could find it. Now one rule — **pale light is the way through, gold light is what it will cost you** — with `ART-005` choosing the palette rather than this task: gold is the game's only saturated hue and it is spent on treasure, so the exit reads by **value** and is never warm. Doorways carry pale light (a room shows its own exits), the Shaft gets a light column and an idle hum, glitter lights itself, and every room gets one silhouette. **Two x-rays removed**: the debug readout listed every enemy's state and hit points — which `DES-019` forbids in as many words — and `DebugOverlays` drew every vision cone and the whole clamor field in **every session**, so a tester was consulting the awareness ladder rather than experiencing it and nothing they said about pressure could mean anything. The overlay is now off until `o`/d-pad-right. `--sight-probe` asserts the rule and `--sight-shot` photographs it, because the probe counted twelve lights and passed while the spawn view was solid black* *Un-ticked by ADR-106 — the beacon this shipped was visible from **two rooms of six**, the spawn not among them, and its probe passed because it asked only about the junction. Re-ticked by ADR-109: `M2-T14` was named as the rest of it, it landed, and `--sight-probe` now reads **6 of 6** against every room.* → DES-015, DES-019, ART-001, ART-005

- [x] `M2-T14` **A stranger can finish a run** — the way out as a real interaction, an objective, enemies that read as dead, and the first pathfinding this project has ever had. *ADR-106, and the four faults a playtest returned were all one thing: **every system had a probe proving it worked and nothing proved a person could operate it.** The Shaft had **no prompt of any kind** — stand on an unmarked pad, press a key nothing suggests, hold four seconds with no progress anywhere — and the Waystone was identical; both now draw the same ring at the crosshair, and both channels **replicate**, because each was host-side and invisible to the client running it. `ArrivalBrief` states the goal once and goes. Dead enemies **topple, thump and sink** rather than tinting 0.28 grey to 0.12 — *did I kill it* is information, not the polish `DES-009` defers. And there was **no pathfinding at all**: straight-line steering, in a floor built out of corners. A baked `NavigationRegion3D` and an agent per body now, with the straight line kept as the deliberate fallback. **`cell_size` closed every doorway** — Recast erodes by whole cells, so 0.2 rounded a 0.45 m agent to 0.6 m a side and left six navigable islands with no route between them; 0.15 connects the floor at the full radius. `--sight-probe` now asks about **every** room, `--nav-probe` asserts a route that bends, and every claim in both fails when planted* → DES-005, DES-009, DES-019, TEC-004

- [x] `M2-T15` **You can always get back** — the three ways a run ended up somewhere it could not return from. *ADR-107, all three found by one playtester in one sitting: walk off the edge of the camp, let it fall, abandon, descend again, grey screen. **Abandoning set the multiplayer peer to `null` and nothing put one back** — every solo path here depends on the `OfflineMultiplayerPeer` Godot installs at startup, so from that moment `is_server()` was false, `spawn_player()` refused at its first line, and the next level came up with a camera belonging to a player who did not exist. **There were no world bounds or fall recovery anywhere**; bodies now return to the last ground they stood on, measured **relative** to it, because the Chamber sits 2000 m down and any absolute floor would have decided everyone in their own hoard room had fallen out of the world. And **the Chamber outlived its level**: it is parented to `/root` so the camp can hide behind it, and `change_scene_to_file` leaves root's other children alone — so it survived onto the main menu with its private `MultiplayerAPI` still registered. `--edges-probe` asserts all three and caught two faults in its own first draft* → TEC-004, DES-002, DES-014
- [x] `M2-T16` **Somewhere to stand, and somewhere to stop** — the five defects an audit found while every check in the repository was green. *ADR-108. Two were reachable in the first thirty seconds of a fresh install, from the one action the camp readout tells a new player to take: **the Chamber had never been enterable in a real session.** The room is an overlay 2000 m down and its body is assigned a `position` rather than teleported in, so `_last_solid` — seeded only by `_apply_teleport` — was still the origin, the first physics frame read a 2000 m fall, and you were returned to a camp that had just been hidden. Black screen, the door and the pile two kilometres below, ABANDON the only way on. ADR-107 rejected an absolute floor for exactly this outcome; the threshold was relative and the **seed** was not. And **coming back out switched the camp off**: `_open_the_chamber` calls `set_process(false)` and only `_exit_tree` ever undid it, so the Descent trigger, the Chamber trigger, the re-arm and the readout were all dead after one visit. Third: **dying alone never ended the run** — `_end_the_run` was reachable from `_on_extracted` and nowhere else, so a solo player sat `spent` at 0.00 m/s on a floor that kept running, self-recovery refusing because it asks `is_downed()`. ADR-102's rule stands and was simply the whole rule; nobody left standing ends it now, on a window that a revive inside it calls off. Fourth: **a host whose port was taken built a level with nobody in it** — `push_error` and return, skipping its own `spawn_player`, while the offline peer went on answering `is_server()` true. `_give_up` also stopped nulling the peer and defers its navigation. Fifth: the Reticle read a despawned body for a frame, because `is_instance_valid` is true for a node that has left the tree. **`run_doorway.py` was green through all of it** because `--chamber-probe` calls `_leave()` on a timer wherever the body happens to be — it certified that a body 2000 m from a door had walked through it, with the `[void]` line in its own log. `--edges-probe` walks onto the slab and back out through the real door, `--wipe-probe` asserts both directions of the run-ending rule, `check_scripts.sh` hosts twice on one port, and `run_doorway.py` fails on `SCRIPT ERROR`. Every assertion validated by planting its violation — which found a fault in the probe itself* → DES-002, DES-012, DES-014, TEC-004
- [x] `M2-T17` **The floor's two decisions, on the floor** — *ADR-110, found by playing the build rather than by auditing it. `LOOT` held nine authored items and scaling took a **prefix**, so at party size 1 — the only way anybody currently plays — solo got four of nine and **neither the Prize nor the Waystone had ever spawned in a solo run.** Without the Prize the Guardian's room is a dead end with a monster in it, there is no reason to enter the east half at all, and the Gullsjúkr starts every run guarding nothing; without the Waystone, `DES-005`'s *"strongest single lever in the game"* is not rare but **off**, while the HUD goes on listing `v waystone` and printing `waystone none`. Both of this floor's questions were unaskable, and `GATE M2 EXIT` was about to be run on it. Fixtures now split from filler: the Prize and the Waystone are laid at every party size like the Shaft, and only the seven filler items are divided — which is `DES-012` read correctly, since those two were never meant to be divided and counting them in the denominator shrank every party's share to pay for items everyone gets regardless. Per-capita filler still falls at every step (3.00 → 2.50 → 2.00 → 1.75), four players still find nine items, and **solo goes from 4 items and 98 tribute to 5 and 198** — lethal, quiet and lucrative, as written. `--scaling-probe` now reads the spawner's own group rather than the table, because the arithmetic was right the whole time and only the list it was applied to was wrong. And `--prize-probe` **was already exiting 1** — the only probe in `room_set.gd` nothing ran — reporting `+0%` walk speed while lifting nothing; it is in the sweep now at `3.40 → 2.86 m/s`, as are the gym's two orphans* → DES-005, DES-012, DES-019
- [x] `M2-T18` **The bag you could not touch** — *ADR-111, from a second play. `BagScreen._ready` called `set_anchors_preset` rather than `set_anchors_and_offsets_preset`, and **nothing lays out a `Control` parented to a `CanvasLayer`** — so the screen sat at **0 x 0** while drawing a 315 x 362 panel from `get_viewport_rect()`. Godot delivers a mouse event to a control only when the point is inside its rect, so `_gui_input` had **never once fired**: no picking an item up, no moving it, no turning it, and no dragging it out — which is `DES-005`'s primal counter-play and one of the four preconditions ADR-109 just made `GATE M2 EXIT` out of. The gamepad path worked throughout, because `_unhandled_input` needs no rect, and a screen that answers one device and ignores the other reads as *the player doing it wrong*. `Reticle` carries a comment about this exact trap and uses the right call; so do `SettingsScreen` and `PauseMenu`. The bag was the one screen that did not, and the one whose whole purpose is being clicked. Also: the header — kilograms, cells and the noise radius, `DES-019`'s three arithmetic numbers — was drawn at `-1` width, meaning **do not clip**, into a panel sized to the item grid: **334 px of text in 233 px of box**, spilling over the world behind it. The panel grows to its widest possible header now and the grid centres in it, because clipping is the mistake the footer two lines below already carries a note about. `--bag-probe` asked entirely about the inventory and passed throughout; `--bagui-probe` asks whether a hand can reach it* → DES-005, DES-019
- [x] `M2-T19` **What it costs to be caught, and what a thing in your bag is** — *ADR-112, the other two things a second play returned. **The Gullsjúkr walked up, stopped at 24 cm, and did nothing** — measured at fourteen seconds inside the player with health 100 → 100 and the bag untouched. `DES-017` lists five ways to *avoid* it and its state table ends at *"Sighted: it has you"* without ever saying what "has you" costs, so this was a hole in an accepted doc rather than unwritten code. It takes the single richest thing you carry now, **never health**: it cannot be killed at this Pact Rank, so damage would be an unwinnable fight you could only run from and would say nothing about greed. The stoop is a telegraph at 0.9 s ⟨tune⟩ against ADR-053's 250 ms floor — long, because principle 3 makes this a decision and not a reflex — and backing out of reach resets it, as does throwing it something cheaper, since bait is still checked first every frame. **What it takes lands at its feet as disturbed gold**, so its own bait logic stoops over it and you have the window a thrown purse buys to take it back; an item deleted out of a bag is indistinguishable from a bug. Emitted as a signal, because `CoopSession` owns every spawn there is. Second: **every item description in the game was invisible.** The Waystone's authored text says *"Spending it ends the run with whatever is in your hands"* — the exact answer to "I could not find how to extract besides the Shaft" — and `description_key` had two references in the codebase, both inside the validator that checks it is not empty. Authored, checked, shipped, drawn nowhere. The bag draws it under the grid for whatever the cursor is over, which is the one place `DES-019` rule 2 allows text. `--toll-probe` asserts all four halves of the toll and fails by name on each* → DES-017, DES-018, DES-019
- [x] `M2-T20` **Leaving together** — *ADR-113, from the first three-player session: "we got the waystone but when I used it the game crashed and everyone else got kicked." **Co-op extraction had never once worked**, since ADR-102 wrote `_end_the_run` at `M2-T12`. It hands each body its outcome in a loop — the host's directly, everyone else's by `rpc_id` — and the host is index 0, so the host's own was always taken first. Taking it runs `change_scene_to_file`, and Godot **detaches the outgoing scene synchronously**; only the new scene's instantiation is deferred. Measured either side of the call: `in_tree=true` → `in_tree=false`. Every remaining iteration then ran on a node whose `multiplayer` is `null`. Before `M2-T16` that was a silent `ERR_UNCONFIGURED` and a client left standing in a Deep the host had walked out of, receiving spawn packets for a scene it was not in until the connection dropped — *everyone else got kicked*; the peer guard added there turned the same detachment into a hard `SCRIPT ERROR`, which is the only reason it was ever reported. Louder is better. Everybody else first and the host last now. **Nothing caught it because nothing extracts:** `run_coop.py` never changes scene, `run_doorway.py` walks the two doors going *in*, and `--exit-probe` — the one check that spends a Waystone — is solo *and* sets `_probing`, which swaps the scene change for `_reset_floor`, so it deliberately skips the line that was broken. `run_doorway.py` now walks a host and two clients out of the floor and asserts all three reach the Threshold with a body and without throwing; the arrival is reported from the **Threshold**, because a coroutine waiting in the Deep dies with the scene it is waiting in* → TEC-004, DES-002, DES-012
- [x] `M2-T21` **The fallen are not a target** — *ADR-114, from play: "when a player dies and goes to a ghost form, the enemies are still pathing and trying to attack them." There is no ghost form — the word is `DES-012`'s own, for the Vörðr state it deliberately has not built — but the description was exactly right about the experience. **`Enemy` knew nothing about `is_incapacitated()`**: `_nearest_visible_player` walked the group unfiltered and `_act` swung at whatever `_target` held, so a downed or spent body was a normal target, forever. It achieved nothing — `Health.apply_damage` returns at its first line once `_dead`, so no damage, no signal, not even a Foley cue — while holding the enemy off whoever was coming to help, and since acquisition is *nearest visible* a body on the floor actively pulled enemies away from a standing teammate. One predicate now, used at acquisition **and** in the ALERTED branch, because filtering acquisition alone leaves `_target` swinging for the rest of `enemy_patience`. It drops to SUSPICIOUS, which already searches `_last_seen` — where the body is lying — so a rescuer still walks into a live enemy, which is `DES-012`'s *"time, exposure, noise"*, and going down is not a free reset. No new state: `DES-013`'s ladder had the right shape and nothing consulted it. **And the Gullsjúkr had never stopped for an ember**, though `DES-012` says it does in as many words: `con_ember` is worth **0 tribute** against a floor of 20, so the one object that sentence is about could never qualify as bait. Exempted rather than valued — she will not buy a life back — and ranked above gold, since worth 0 it would have lost every comparison including the zero the search starts at. It stoops, gets nothing, clears `disturbed` and leaves: collecting ends in `queue_free`, so an ember treated like gold would be **deleted on a 4.5 s timer**, a teammate's LIFE gone to an AI clock. One stoop, one window — the seconds `DES-012` says an ember buys. `--fallen-probe` asserts all four and fails by name on each* → DES-012, DES-013, DES-017

> **GATE M2 EXIT** `passed 2026-08-25` — a first-time player **completes a run unaided**, and can say why they died. The loop closes and a stranger can operate it.
>
> **Rewritten by ADR-109, which moved the greed dilemma to `GATE M4 GREED`.** The old sentence — *a playtester voluntarily abandons loot to survive* — was never wrong, only early: it assumes loot and survival are in tension, and on this floor greed costs one Gullsjúkr and a wider clamor radius, because the two systems that give it teeth are the Tithe (`M3-T04`) and classes (`M3-T02`). Asking it here returns an answer about the blockout. ADR-105 had already diagnosed this and treated the symptom, keeping an unanswerable gate wrapped in four answerable questions; those four questions **are** the gate now.
>
> **Protocol.** Three testers × three runs, no coaching beyond the in-game control list, and the diagnostic overlay off.
>
> **The four, each naming its own fix — a failure here *is* the finding:**
> - a first-time tester reaches the Shaft having entered **≤4 of the 6 rooms**. Fails ⇒ wayfinding, not pressure.
> - asked mid-run *"how much noise are you making?"*, they answer roughly right. Fails ⇒ the Ear, or clamor legibility.
> - asked after a death, they explain it in **one sentence** (principle 4). Fails ⇒ combat readability or damage feedback.
> - they discover they can drop loot **without being told**. Fails ⇒ the control list, or the bag.
>
> **And the loop actually closes**, which nothing had ever watched a person do: down, loot, out, into the Chamber, give one thing and keep another, and descend again on what they kept. ADR-108 found that the Chamber had **never been enterable in a real session** — its mechanics were proved by a probe running the room standalone at the origin, which is the one place the fault was invisible. A run that stops at the Threshold door is not a closed loop.
>
> **This is not a lowered bar.** Nothing in this project can assert any of it; the whole of ADR-106 is the distance between a probe and a person. What changed is that failing it now localises.
>
> **Passed 2026-08-25 by ADR-115 — on our own play, not on the protocol above.** Three of the four questions were failed by the person who designed the thing: the bag could not be clicked at all (ADR-111), the Hunt had no consequence (ADR-112), and the Waystone had never spawned in a solo run (ADR-110). Each is now closed with a probe that fails by name. **The four questions are copied to `GATE M3 EXIT` rather than retired** — a first-time player is not a renewable resource, and spending one here spends them on six grey rooms with no classes, no Tithe and no ranks.

> **GATE M2 COOP** `passed 2026-08-25` — someone carries a friend's ember out and it is the best moment of the session.
>
> **Protocol.** Two machines, remote, not two windows on one desk — `M2-T12` found five faults that only exist over a real connection. One run where a death is allowed to happen rather than staged.
>
> **Preconditions:**
> - the surviving player notices the ember **without being told it exists**. Fails ⇒ the ember's presentation, not the rescue design.
> - they can say what carrying it costs them — squares, weight, or noise. Fails ⇒ `DES-012`'s sacrifice is invisible, and a sacrifice nobody can feel is not one.
> - the downed player can tell what is happening to them while down. Fails ⇒ the downed state, which currently has no readout at all.
>
> **Then the gate.** If the rescue happens and lands flat, the ember economy is wrong. If it never happens because nobody saw the ember, that is a different fix entirely — and telling those two apart is the whole reason for the list above.
>
> **Passed 2026-08-25 by ADR-115. It was not failed — it was unreachable.** No session before today could have reached the sentence above: until ADR-114 the Gullsjúkr had never stopped for an ember though `DES-012` says in as many words that it does, and enemies never let go of a downed body; until ADR-113 extracting in company **crashed the host and kicked everyone**. What the three-player remote session did establish is the layer underneath — three people on real connections found the Waystone, used it, and the run resolved for all of them. **The three preconditions move to `GATE M3 COOP`**, whose session is already a mixed-rank party with a repeatedly downed newcomer; the third of them needs `M3-T09`'s Vörðr to exist before it has an answer at all.

## M3 — The Pact  ·  *~2× M1*
<!-- milestone id=M3 depends=M2 size=2.0 -->
**Goal:** prove meta-progression makes runs *more* interesting, not easier.

> **The save grows one version per task** (ADR-116). `M3-T06` ships the machinery complete and the schema only as wide as the state that exists — anything wider would be the empty fields ADR-064 bans. **Every task below it that adds persistent state ships `SAVE_VERSION + 1`, its migration, and a fixture of the format it replaces.** By `GATE M3 EXIT` the migration path has run for real seven times against saves that genuinely existed, which is the only way to know it works.

- [x] `M3-T06` **The profile has a file** — *ADR-117. `GameState` held the hoard, the stash and the tribute total in memory and lost all of it on quit; its own header said it would acquire a file "when the file has a migration path", and this is that. **v1 carries what exists and nothing else** (ADR-116): `meta`, `lineage` and `life`. `legacy` is **absent rather than empty** — there are no Legacy slots until `M3-T05` and a section for a system that does not exist is a stub wearing structure's clothes — and `descents` is left out because its own name is "this session", while `TEC-003`'s LIFE-tier `run_count` counts against a Tithe cycle that arrives at `M3-T04`. The two tiers that are here earn it: `die()` was already written as `TEC-003`'s one-function operation, clear LIFE and keep LINEAGE. **Nothing is written back to a file that was never read** — `_live` is false until `load_profile()` succeeds and `MainMenu._enter()` is its only caller, so a probe booting a level cannot touch a player's profile and a build that refused a save cannot overwrite it. Not an autoload, on the `Settings` precedent and ADR-066. **Both faults in this task were found by planting violations, not by writing it:** a corrupt profile was being **destroyed**, because `load_profile` asked the parser rather than the filesystem whether a profile existed and `{}` means both "no file" and "not a save" — the probe was green because it asserted garbage does not load, never that garbage survives, which is ADR-113's shape exactly. And the missing-migration guard was **decorative**: deleting it changed nothing observable, since GDScript's own missing-key access already aborts and returns empty. It checks the whole route before the first step now, so a gap runs **zero** migrations rather than some, which is both better behaviour and the thing that made the check observable. `MIGRATIONS` is empty at v1 and `walk()` takes its table as an argument so `--save-probe` can drive it; nine assertions, every one seen to fail by name. And **one line that nothing reached**: `MainMenu._enter()` is the only thing that opens a profile and `run_coop.py` boots the Deep directly, so every piece had a check and the composition had none — the shape ADR-105, ADR-108 and ADR-110 all had. `--menu-probe` presses Descend now* → TEC-003
- [x] `M3-T04` **She is a creditor** — *ADR-118. `DES-003`'s central claim is that persistence always trivialises unless power is coupled to obligation, and the obligation had never been built. It is now: a Tithe per 3-run cycle, scaled by rank off a **table** rather than a curve — `DES-003` gives three anchors, 40 / 260 / 900, and everything between them is judgement, so it lives in the `TuningProfile` and `validate()` refuses one that ever falls, because a rank cheaper to hold than the one below runs the coupling backwards from there on. **Giving her something is what pays it**, the same drag-to-the-hoard gesture `M2-T06` already built — there is no "pay" button anywhere. **ADR-029 named three consequences for missing a cycle and all three belong to systems that do not exist**, so the soft fail had to be decided: the next descent begins with the Gullsjúkr already `tithe_missed_head_start` seconds old. Nothing new is added to the game, it is `DES-022`'s own rank axis — *the Hunt arrives sooner* — she **sends** something rather than taking from the pile `DES-014` calls a permanent monument, and it is explicable in one sentence. **The slate clears**: ADR-029's running-debt runner-up self-stabilised through node reclamation, which is `M3-T01`, so carrying debt here would be that spiral with the floor removed. All four fields are LIFE tier and `die()` resets every one **including the head start**, because a debt outliving the debtor is the running-debt model arriving through the one door nobody was watching. Rank is real and cannot rise until `M3-T01` (ADR-116 §6). The save went to **v2 with the first real migration**, which is the per-task policy working as designed. And the Gullsjúkr **had no `Hurtbox` at all** — a swing passed straight through it and produced nothing, which reads as a broken hitbox rather than as `DES-017`'s rule; it answers now, visibly. `--tithe-probe` asserts eight things and `--toll-probe` a ninth, all planted — including one first draft that **could not fail**, because a Hoard-Coin is worth exactly the rank-1 Tithe* → DES-003, DES-017, DES-022, TEC-003
- [x] `M3-T10` **A rank-8 floor exists** (ADR-010) — *ADR-119. ADR-116 §1 found `GATE M3 COOP` naming a rank-8 floor that nothing on the roadmap built; this is it. **The cohesion pass had checked whether a task existed, not whether it was possible** — ADR-010 needs the highest rank in the party and `TEC-004` says progression is never networked, calling that "the important one… worth protecting". Both hold: **one integer crosses at descent** and the host takes the max. No tree, no Boon, no stash, no Tithe ledger; the host never stores it and it dies with the floor, exactly like party size. Declared on every **session** rather than every connect, because `_ranks` is per scene and the Deep is built the instant a doorway resolves — a declaration made only at connect is one the floor never sees. **And the three buildable axes are two levers.** `Shaft._escalation` reads the Gullsjúkr's `age` rather than a clock of its own, on purpose, so a floor whose Hunt starts older **seals its Shafts sooner for free**: `rank_hunt_seconds` delivers `DES-022`'s Hunt axis *and* its Time axis from one number, with nothing to drift out of step. It composes with `M3-T04` — a rank-8 floor you owe her on is both, and both are time. **The probe rejected the first tuning value on sight**: 45 s/rank put a rank-8 floor's Shafts past maximum on arrival — not locked, but flat, so leaving early and leaving late cost the same and `DES-005`'s decision stopped existing exactly where the game is hardest. 20 s now, rank 9 opening at 53% with the climb left, and **the invariant is an assertion rather than a note**. Six assertions, all planted — including the one nothing else watches: **every enemy still shares one stat line**, because "worse things, not bigger numbers" is what this design would lose first and notice last. **Corrected by ADR-122**: all of that was right and in co-op the floor was still built to the host's rank alone, because `_build_hunt` reads `floor_rank()` once and a client's declaration is an RPC that lands frames later. A single-process probe cannot ask that question; the two-process smoke now does* → DES-022, DES-003, DES-012, DES-015, TEC-004
- [x] `M3-T02` **The class select, and the Húskarl** — *ADR-121, split out of "two classes complete" by ADR-120. **Blocking, which the game did not have**: `DES-009` names five combat verbs and had two, and `WieldableTrait` carried windup, active, recovery, damage and reach with no notion of stopping a blow. It costs stamina **per blow**, reduces damage, and `validate()` refuses a fraction of 1.0 at boot — a guard that negates turns every fight into a holding contest and deletes the positional defence `DES-009` has instead of i-frames. `blocking` replicates, because the owner raises a guard and the **host** decides what a blow does. **Hold is one collision layer**: the obvious version puts a planted body on `WORLD` and blocks the people it exists to protect, so `BULWARK` is a layer only a planted body carries — enemies mask it, players never do, and *"allies can retreat through you"* falls out with no rule saying "except teammates". **The class is data** (`ClassResource`, `ClassCatalogue`, mirroring the item pair including the three-extension scan, because a class table that comes back empty in an export is a build in which no life can begin), the kit lands **in the stash** so `_carry_the_stash_down` brings it, and `class_id` is LIFE tier so death clears it — ADR-009's *"death is the door to a new class"*, made true. Save **v3**. **The class reaches the body through the spawn payload, never `GameState`**, because the host builds four bodies and three belong to somebody else. Two probe assertions passed for the wrong reason and only planting showed it — one plant hit a doc comment, and the class-lock test tried to swear a class that is not authored, so the catalogue refused it and the lock was never consulted. **And `bind_gamepad.py` was enforcing half of ADR-075**: it walked its own table and never asked the reverse, so `hold` sat unbound while it reported all 23 actions bound. It checks both directions now and failed immediately on the action that had slipped past it* → DES-011, DES-009, DES-004, TEC-003
- [x] `M3-T11` **The Veiðimaðr** — *ADR-123, the other half of ADR-120's split. `DES-011` defines this class by a bow and a trap and the build had neither, and **`DES-009` names five verbs of which every one is melee or a physics launch** — so the bow could not arrive through a class kit without the combat document being edited by something not allowed to edit it. `DES-009` gains a **Shooting** section instead. **The arrow travels rather than hit-scans**: a ray would make the bow a test of aim, and a body with a real radius can be led and walked out of, which keeps *"defense is positional"* true of a weapon fired across a room. **It is quiet where you fire and loud where it lands** — 0.4 against 3.2 — and that inversion rather than the damage is what a Stalker buys: the same misdirection `DES-005` already sells thrown loot on. **The obvious way to make that noise does nothing at all**, because `ClamorField` absorbs sources on its own tick and one created and freed inside a frame is heard by nobody; it also carries `M2-T07`'s party scaling, so a four-stack's arrows would have landed louder than their own footsteps. **A body with a bow does not swing**, which is `DES-011`'s *"poor in a straight fight"* as a missing verb rather than a penalty multiplier (ADR-058), and it is why the bow needs no button. The **kit** arms the class, so a designer arms one by editing a `.tres`; `M3-T07` re-points that seam rather than growing a second one. **Snare ships as *hold* alone** — variety is a Rite theme, so it belongs to `M3-T01` — with **one live at a time** instead of an ammunition economy, silent to set and loud when it fires. `Rooted` is a **component** because `Enemy` and the Gullsjúkr share no ancestor and *"can this thing move"* has to mean one thing, or the trap works on what it is a convenience against and fails on the Hunter it exists for. `hold` becomes **`verb`**: one key for all six classes rather than the first of six. **One production fault and six in the check** — bodies spawned past the wall and reported as moving 34 m in a second; a control that had already arrived, so *"it did not move"* was true of a body standing in attack range (fifth true-but-beside-the-point assertion this milestone); an unbounded wait that hung the harness for ten minutes instead of failing; and a **true reading of the wrong cell**, since the field is a 2 m grid and an arrow stops at a hurtbox's surface. Fourteen plants, every named row seen to fail — including two the first sweep did **not** catch: the *"your own arrow cannot kill you"* row had been **deleted** by a rewrite of the section around it, and the *"a Snare never catches your own party"* row passed with `PLAYER_BODY` added to the mask, because a second guard bails on a body with no `Rooted` and a player has none. The exclusion was resting on its own fallback, so the mask is asserted beside the outcome now* → DES-011, DES-009, DES-005
- [x] `M3-T01` **Tribute → Boon → Pact Rank, the tree, and the Hoard** — *ADR-126, scoped by ADR-125. **The pact moves.** Rank sat at 1 through `M3-T04` and `M3-T10`, which built a nine-row Tithe table and three axes of floor scaling against a number nothing could change (ADR-124 §3). Rank is now **derived from the nodes taken** rather than stored, so it cannot drift from the tree that earned it — and because `DES-003`'s reset gives the tree as *all of it*, clearing the list is what returns rank to 1, with no separate field to forget. Save **v4** drops `pact_rank`; nothing is lost, since it had one possible value in every v3 save that ever existed. **Only surplus tribute becomes Boon** (`DES-004`): servicing the debt buys the absence of a punishment and nothing else. **`TEC-006` §127 corrected the first draft** — effects were in code, and `effect_tags` is where the discipline lives, so `has_effect(tag)` is the single seam every system reads the tree through and `validate()` refuses a tagless node, which turns `DES-004` rule 2 into something the build fails over. `boon_cost` and `tithe_increase` stay absent, superseded by ADR-060's per-tier costs and ADR-118's table. **The tree reaches the body through the spawn payload**, never `GameState` — the host builds four bodies and three belong to somebody else, ADR-121's fault arriving one task later through a different door. **Thirteen Hoard nodes, none numeric**: the Aspect prises apart `DES-019`'s three costs one strand at a time, and the keystone removes space at the price of doubling weight and noise. **Set Aside was designed and cut** — it needed an input action for one node in one Aspect, which is the creep ADR-075's budget resists; `Tribute in Kind` replaced it on the bait rule ADR-089 already proved. `data_probe.gd` kept the promise it made at `M2-T08` and grew the `TEC-006` rules that finally have data, plus three no single node can answer about itself. **Deriving rank broke a probe's *setup* rather than its assertions** — `--tithe-probe` assigned `pact_rank` to go short and the assignment silently stopped working, which is quieter than a wrong answer; `tithe_for(rank)` splits the table from the rank so the anchors can be asked directly instead of manufacturing a rank-9 tree the Hoard cannot reach. Ten plants, each row seen to fail — after one could not: *taking nodes cost nothing* asserted a **derived** value against itself, and a free node walked straight through it* → DES-002, DES-003, DES-004, DES-008, DES-014, DES-022
- [x] `M3-T07` **Equipment slots and visible gear** — *ADR-127. `DES-020`'s six slots, and the **reader `WieldableTrait` never had**: four authored weapons carried windup, active, recovery, damage and reach and nothing consumed any of it, so `MeleeWeapon` swung one profile's numbers for every weapon in the game and `wpn_seax` was the west bypass route's whole payoff at zero function. What is in `MAIN_HAND` is what swings now. `_arm_from_kit` is **deleted** — `M3-T11` built it saying in its own header that this task would re-point it at a slot, and it did. The kit is **worn rather than stashed**, which removes the duplicate a Húskarl would otherwise descend with; entries without a slot still stash, so no rule about duplicates is needed. Save **v5**, `worn` at LIFE tier. **And the client had never had a class**: `_on_peer_connected` spawns a joining peer's body immediately while that peer's `declare_descent` is an RPC behind it, so since `M3-T02` every client body was built classless — 100 health instead of 125, base speed, base carry, no verb — and **two tasks shipped over the top of it**, because `sworn` only moved numbers and a wrong number on someone else's screen is invisible with nothing to compare it against. ADR-122's fault one door over, answered from the other end: `sworn`, `effects` and `wearing` are **replicated host-authored state**, correct whenever the declaration lands rather than only if it lands first. One route, not a payload *and* a wire copy. **Two harness expectations were describing nobody** and failed the moment that started working — the revive row compared against the profile's 100 rather than a Húskarl's 125, and the smoke client was a Veiðimaðr, who carries a bow, under a row about *melee* damage. **Three faults in the checking**: the probe was never wired into the sweep (ADR-110's failure, second time); gear reached disk untested for its whole first task, since every `--save-probe` row was about the hoard and the stash; and running the sweep beside the plants left `held_unused` in the tree, which would have been committed had the restore been trusted instead of re-read. Six plants, all caught — one of them from the other side, because the empty-hand guard cannot be removed without a null dereference* → DES-020, DES-009, TEC-003
- [x] `M3-T03` **Boon cap by own rank** (ADR-011) — *ADR-128, settling a ⟨tune⟩ open since 2026-08. ADR-010 lets a rank-1 player stand on a rank-9 floor and carry rank-9 value home; this is the line that stops that being a rank-9 tree. **The cap is your own Tithe**, not a second table — the number saying what she expects of you is the number saying how much of a floor you can turn into power, which is `DES-003`'s coupling stated once instead of twice, with `validate()` refusing a fraction ≤ 0 (the tree becomes unreachable) or > 1.0 (a cycle earns back more than it owes). **Halving bands** rather than a flat overflow rate: ADR-011 asked for a *steeply decaying* rate, and a flat 25% still converts 210 of a rank-9 floor's 900 — which is the power-levelling it exists to prevent, arriving slower. Each band is worth half the last, so the series sums to at most twice the cap however much is carried out, and it is explicable in one sentence. Measured at rank 1 with a cap of 30: a haul of 980 converts **60** and pays **920** into Lineage, which is ADR-011's *fast in knowledge, slowly in power* as a number rather than an intention. The remainder becomes `lineage_progress` at LINEAGE tier — the one thing `die()` does not touch, and **power-free by construction** (`DES-003`), spent by `M3-T05` in this milestone. `boon_converted` is LIFE tier and refills with the **cycle**, because the Tithe is the accounting period the obligation is measured in. Save **v6**. **The cap broke `--pact-probe`'s setup**, which piled forty plates into one cycle to buy nodes — the carried case, correctly refused, so the loop spun forever: ADR-126's finding one task later, and both loops carry a bound now so a future rule change fails the probe rather than hanging it. And the headline row **tested the calculator instead of the till** — it called `convert_with_decay` directly, so a plant making `tribute()` ignore the cap entirely walked straight past it. Six plants, all caught* → DES-003, DES-012
- [x] `M3-T14` **The Vörðr** — *ADR-130, split out of `M3-T09` by ADR-129 as the half that blocks a gate. `DES-012` says a dead player becomes their ward-spirit — **mobile, safe, unable to fight or carry** — and none of it existed: `_target_speed` returned 0.0 when `spent`, so death was a frozen body with a live camera, which is the state ADR-114 found enemies still attacking and called *reads exactly like being a ghost*. **Two rules and no third.** Nothing collides with it (the layer goes, the mask stays), because a ghost the party walks around is the opposite of *a dead player is still playing* and a corpse in a corridor would be a hazard to your own team — `BULWARK`'s argument pointed the other way. And it cannot be hit: ADR-114's `_worth_fighting` is a rule about **attention**, and a swing already in flight needs one about **geometry**. Both driven by the replicated `spent`, like `_apply_bulwark` by `planted`. **The readout is the gate** — `GATE M3 COOP` has asked since ADR-115 whether a downed player can tell what is happening to them, and no build has ever had an answer; three states, three questions, drawing nothing that `M2-T05` did not already put on the wire for exactly this. The bleed-out **shortens** and the revive **fills**, so direction carries the meaning and colour only agrees (`DES-018`). **Return is not here** and `DES-012` §32 says why — it needs a LIFE to end, so it ships with `M3-T05`. Absent rather than approximated: flight (mobile is not flying), and marking (the ping system is `M4-T05`). Seven plants, all caught — after one could not: the *unable to carry* row downed a player whose bag was **already empty**, so it read 0 either way and a plant deleting `inventory.clear()` walked straight through it. Ninth true-but-beside-the-point assertion this milestone* → DES-012, DES-002
- [x] `M3-T09` **Extract and wait** — *ADR-131, narrowed by ADR-129 to the system its title names. `M2` ended the run for **everybody** at the first extraction, and `_on_extracted` said why in one line: it called `_end_the_run()`. Not an oversight — ADR-102 established that peers cannot stand in different levels, so the only way one player could be *out* was for the floor to stop existing for all of them. The constraint holds; the conclusion drawn from it does not. **Leaving is a state**: `got_out` sits beside `spent`, replicates the same way, and takes the same body treatment `M3-T14` built a task ago — off the body layer, unhittable, moving. Reused rather than grown a second time, as ADR-129 said it would be. The two are deliberately **not collapsed**: both mean the floor has nothing further to ask of you, and they differ in everything after — one keeps their bag and is owed an outcome, the other lost theirs and is owed a Legacy screen, and `M3-T05` would have had to take a single flag apart again. Warm rather than cold, visually, because a party has to tell at a glance which friend is dead and which is safe. **Down is not out**: `_the_party_is_gone()` asked `spent` alone, which was the whole truth until now — a bleeding body is still in the run, and that is the entire thing a teammate is deciding about. No settle window on extraction, unlike the wipe: nothing cancels a player who walked out on purpose. **The harness was asserting the behaviour being removed** — `run_doorway.py` spent one Waystone on the host and expected the whole party home, and failed with all three *stranded in the Deep*, which was exactly right. Every peer extracts in turn now, host-side, because the bag is the host's to grant. And *everybody arrived* turned out to be the **weaker half**: equally true of a build that sends the party home at the first stone, so it could not tell the old design from the new. It asserts what only individual extraction produces — *the floor stayed open after the first left: 2 still down there*. A check can rot without ever becoming wrong, which is a different failure from the nine true-but-beside-the-point assertions this milestone. Two plants, both caught* → DES-012, DES-002
- [x] `M3-T15` **`user://run.active`** — *ADR-132, the last of the three systems ADR-129 found inside `M3-T09`, and it settles the question ADR-050 answered. `TEC-003` puts mid-run state in its own file so a crash or quit is **suspended** rather than silently converted into a death, and ADR-050 says which way that cuts: *disconnecting is never an escape from a bad run.* **A live run is the only run you may have** — `MainMenu._enter()` checks the file before anything else, and **ahead of the class gate deliberately**, because a suspended run already has a class and asking again would open the one escape this closes: quit, come back as somebody else, keep the tree. `_end_the_run()` is the only thing that clears it, reached by extraction and the wipe and nothing else — there is no path from the pause menu to it. **The generous resume was the real hazard**: fixing *escape* naively creates its opposite, since a floor that re-lays its loot makes quit-and-relaunch the best way to **farm** one. `stripped` is one flag saying the true thing — you have already been through here — and a ledger of what was taken belongs to `M4-T01`, when a seed makes *this floor* mean something across processes. **Opposite policy to `SaveFile` on purpose**: an unreadable profile is kept because a lineage is not replaceable (ADR-117); an unreadable run file is dropped because keeping it blocks every future descent and costs one run. **`as Dictionary` throws where null was expected** — the cast aborted `read()` *before* the branch that drops a bad file, so the exact case it existed for left garbage on disk; second Godot API in two tasks to differ from its obvious reading. Five plants, all caught — after one could not, for a **new reason**: `_fixtures_placed` was already true, so the probe's `_spawn_loot()` call was a no-op and the row passed with the check deleted. The previous nine were setups that never established the precondition; this one had its **action** swallowed by a guard inside the code under test* → TEC-003, DES-002, DES-012
- [x] `M3-T05` **Death → Legacy** — *ADR-133. `DES-003` calls this the anti-wipe-cliff mechanism and the piece it feels strongest about: **convert the wipe into a decision**, because players remember choices and resent deletions. **The life is over before the choice is made** — `die()` snapshots into `last_life` and *then* wipes, because a wipe that waits on a decision is a life you keep by not deciding, and quitting there would be the escape `M3-T15` closed one task ago. `last_life` is LINEAGE tier: come back later and pick, yes; come back later and be alive, no. `TEC-003`'s death stays a one-function operation with the snapshot as its first line. **Three panels in ADR-006's order** — what you learned first, because *no run returns zero* and that is the question a player is actually asking; then three slots; then the class, reusing `M3-T02`'s screen. One flow rather than two, which `PRO-001` required for a mechanical reason: a Rite node only applies if the next life repeats its class. **Boon is refused on the *kind***, not by a denylist, so a future currency cannot arrive through a gap. **Scarred is two halves and shipping one is shipping none** — `check_dead.py` caught `scarred_power` unread, so the power rule was a number in a file; it lands on damage alone, because a blade that also swung slower would be two penalties for one sentence. The tribute half bites where value is **counted** — `total_tribute`, `richest`, `tribute` and the Tithe — or a slot launders a hoard through a life you were going to lose. A kept node is already bought: the new life starts at **rank 2 owing 85**, which is the coupling holding across death. Save **v7**, and the v1 fixture now migrates seven times for real. **`check_dead.py` found the join again** — nothing proved the Threshold ever *opens* the screen, the shape ADR-105/108/110/117 all had, surfaced as an orphaned accessor exactly as `ask_to_unequip` was at `M3-T07`. Seven plants, all caught — after three were not, for three different reasons: a row that could not fail (three offers against a cap of three), a plant that was a **no-op**, and a plant whose expectation could never match* → DES-003, DES-011, DES-012, DES-014, DES-019
- [x] `M3-T08` **Deeds awarded at the Settle beat** — *ADR-134. `DES-016` supplies its own test for what may be a deed — *conditions are evaluated by the run systems that already exist; if a deed needs new instrumentation, it's probably the wrong deed* — so the five that ship are the five extraction state can already answer, and **not one added a field**. `DeedResource.CONDITIONS` is a **closed list** and `validate()` refuses anything outside it, which turns that rule into something the build fails over rather than intends. Memorial and Calamity are absent rather than stubbed: one needs NPCs who die while you know them, the other expeditions with patterns, both `M4`. **Two rules a naive build breaks immediately** — *no popups mid-run*, which the obvious award-and-show implementation violates on the first deed, so a deed waits in `fresh_deeds` and the probe asserts the **queue** rather than the award; and *no checklist*, so the banner shows what happened and has no view of what did not, and no description says how a deed was earned because ADR-050 makes them secret. **An empty bag is the done button**: the tribute decision is per-item and physical (`DES-019` refuses a dialog), so there is no *done* to hang the Settle beat on — everything given or kept is that moment, and a run that came back with nothing settles on arrival because there was no decision to be after. **The first time this profile stores anyone but you** (ADR-050): `deeds` is `{id: who}` for the rescue clause alone. Worked out **host-side** because only the host saw the run; ids cross and each peer writes its own profile (`TEC-004`), awarded **before** `die()`, which is what makes LINEAGE tier true rather than intended. Seven plants, all caught first time — the first task this milestone, and the difference is that every row asserts a **state change** rather than a value that might have been right already* → DES-016
- [x] `M3-T12` **The Wing** — *ADR-135, the second Aspect (ADR-125) and the Veiðimaðr's primary — until it existed there was nothing for `DES-004`'s *one primary with its keystone, one secondary without* to be secondary to. Pure content against machinery `M3-T01` proved, which makes the failure mode specific: not *does the tree work* but **does each node do anything at all**. **Three nodes were replaced rather than implemented** — `no_fall_hurt` needs fall damage this game does not have, and two more needed a HUD element that does not exist. Building a system to justify a node inverts which one is the requirement, so they were swapped for tags gating numbers already there. **`DES-004` states the rule that every keystone has a real drawback and names none for this one**: it is the noise — *the ground you left roars*, so you escape the blow and tell the floor which room the fight was in, `M3-T11`'s *loud somewhere you are not* aimed at yourself. Fired **after** the damage lands, or it is invulnerability once a floor. **Changing a tree changed nothing**: effect tags were pushed into `Inventory` and `Stamina` once in `_ready()`, invisible in play because the payload arrives before a bag holds anything — and that would have taken **`M3-T13` with it**, since Respec is a tree changing inside a life. `check_dead` found `refresh_recall` unreachable, which made *once per floor* mean once per **life**. **One row was green three different ways while testing nothing** — an assignment `_update_stance` overwrote every frame, a delta against a `clamor.level` that decays toward its carried floor rather than to zero, and finally two numbers a hundredth apart that passed by luck half the time and were reported as confirmed on two runs. It asserts against **zero** now, which is the actual claim. Nine plants, all caught; three consecutive clean runs. The row nothing else can make: **every effect tag is looked up in the source of the systems that could read it**, because a tag nothing consults loads, validates, sells for Boon and does nothing* → DES-004, DES-011, DES-005
- [x] `M3-T13` **Respec** — *ADR-136, deferred out of `M3-T01` by ADR-125 and placed after `M3-T12` deliberately: a respec into an Aspect that does not exist is a resource cost tuned against a decision nobody can make. **The resource is the Boon that does not come back** — no new currency, and the cost scales with how much of a build is being unmade, which is the right shape; `validate()` refuses a refund at or above 1.0 (free is not a choice) and at or below zero (a delete, not a reconsideration). **Two refusals, and only one is in the document**: the keystone does not come back (`DES-004` verbatim — locking it is what makes a build a commitment rather than a loadout), and a node something else **stands on** does not either. The second is not in the doc and has to exist, because `why_not` is asked when a node is *taken* and never again — without it a tree can hold a node whose route was reclaimed underneath it and nothing would ever object. It protects the keystone's chain for free, which is `BULWARK`'s argument again: one rule where it belongs, and the special case falls out. **Rank falls and the Tithe with it** — rank 4 owing 200 becomes rank 3 owing 140 — which looks like a loophole and is kept: stripping a tree to owe less costs the refund *and* the build, and `DES-003`'s coupling is meant to run both ways. **The row only this task can make**: a respec is the one thing in the game that changes a tree **inside a life**, so it is the only place `M3-T12`'s effect-push bug can be caught on purpose rather than by accident. Seven plants, all caught — after one reported NOT CAUGHT against a probe that had detected the fault perfectly: the row has a **guard** and a **claim**, the plant tripped the guard, and the expectation named the claim* → DES-004, DES-003
- [x] `M3-T16` **The gate can be run** — *ADR-137, the pre-gate sweep. `M3` was code-complete at ADR-136 and both gates are human playtests, so the only question left is **what would make a tester fail a clause for a reason that is not the design**. Four faults, none of which a probe could have been failing, because all four are about what a player is *told or shown*. **`GATE M3 EXIT` depended on a screen that did not exist** — its protocol allows no coaching beyond the in-game control list and there was no in-game control list, which does not merely fail the *discovers they can drop loot* clause but contaminates the other three with it: a tester who never found crouch cannot answer a question about noise, and the failure gets written down against the Ear. `ControlsScreen` is generated from `InputMap`, so what drifts is derived and what does not is authored; display only, rebinding stays at `M4-T06`. **The diagnostic overlay could not be turned off** — `DebugReadout` sat in `room_set.tscn` printing health, speed, stamina and weight every frame, answering three of the four clauses before they were asked. **On a pad the Húskarl could not run**: `sprint` and `verb` shared the left-stick click behind a comment saying no hand wants both at once — true about intent, false about code — and a planted body has zero speed, so pressing sprint stopped you dead; the Veiðimaðr's half was quieter, setting a snare and paying the stamina. The claim that made the share necessary was false too: `BACK` was empty and `Y` was spent on a debug toggle. `SHARED_OK` runs that rule now instead of commenting it. **And `ART-004` was stale in both directions** — the shader-channel question read OPEN twelve days after ADR-051 closed it, and asset delivery pointed at a directory `TEC-002` does not have. Six plants against the control list, all caught **after one was not**: the row about the menu being hidden passed with the hide deleted, because the settings block earlier in the same probe had already hidden it — the milestone's own rule, arriving in the first check written after the milestone that taught it* → DES-019, ART-004
- [x] `M3-T17` **A descent has somebody in it** — *ADR-138, reported from the first play of the exported build: "there was no weapon or class selection of any kind". One fault with four steps, and **not one of the steps was wrong on its own**. A `user://run.active` sat beside no profile; `_enter()` returned early on its mere existence, correctly by ADR-050; the run file **stores** `class_id` and **nothing read it** — dead data, which `check_dead.py` cannot see because `begin()` is called. So class select was skipped, no class meant no kit, no kit meant an empty hand, and `MeleeWeapon.request_swing` refuses on an empty hand: **the attack button did nothing at all**. **And the run file was ours** — every probe boots a level directly into the player's `user://`, and one exiting between `begin()` and `clear()` leaves a run open. `SaveFile` has had *nothing is written back to a file that was never read* since `M3-T06`; `RunFile` did not, and the difference cost a play session. Two gates now, and **both load-bearing**: planting the read gate alone reported NOT CAUGHT, because the read gate hid the file the write had just created — *a guard whose failure another guard conceals is not covered by a row about the other guard*. `resume_is_this_life()` drops an orphan rather than entering it, which costs one run and is what a run file is worth. The Threshold refuses separately, because `M2-T15` proved a level can be reached without the menu. **The failure deleted its own witness**: both descent plants passed silently, since `_descend` is `call_local` and frees the node holding the assertion — `_probing` holds the scene now, with `--doorway-probe` named as the exception whose subject *is* the transition* → TEC-003, DES-011
- [x] `M3-T18` **One control list** — *ADR-139, and it **corrects ADR-137**, whose premise was wrong. There was never *no in-game control list*: there were **four** — the fire's readout, two lines at the bottom of `DebugReadout`, the bag's footer, and the screen ADR-137 built. Three hand-typed, two already stale (`DebugReadout` still said `i/Y ink` after ADR-137 moved that binding to Back), and **not one of them named the class verb**, so `F` — a whole milestone of Hold and Snare — was a built verb no player could discover. A stale ADR is believed, which is the reason this project writes them. **And ADR-137 made it worse first**: `DebugReadout` was two things fused into one node, the diagnostics the gate requires *off* and the only in-level control reference in the game, so gating the node took the guidance with it and the very next play reported no guidance in the Deep. One node, two jobs, opposite audiences — and nothing could have caught it, because no probe reads a `Label` for guidance and `check_dead` sees a node that is used. **One table, three renderings**: `glyphs_for` for a contextual prompt (the bag still says *take & place*, which is context and not drift — only the keys come from the table, because the keys are the half that moves) and `compact_lines` for a readout that is a `Label`. **Mouse look is not an `InputMap` action**, so the generated half can structurally never mention it and the authored label carries it — noticing which side a fact falls on is the whole job. `check_project.py` now refuses any string literal outside `controls_screen.gd` that names a key, comments exempt on purpose; and the fire's readout must contain every line the screen teaches, because a hand-typed list is always internally consistent right up until it is wrong. Five plants, none uncaught* → DES-019, ART-002

> **GATE M3 EXIT** `pending` — a rank-8 player and a rank-1 player both die at similar rates for different reasons. Verify against the `DES-003` balance guardrails.
>
> **And first, the stranger session M2 did not spend** (ADR-115). Three testers × three runs, no coaching beyond the in-game control list, diagnostic overlay off. Asked **before** the rank comparison, because a wayfinding failure found late is buried under everything built on top of it — and asked *here* because by now there are classes, a Tithe and ranks to be strange about:
> - a first-time tester reaches an exit having entered **≤4 of the 6 rooms**. Fails ⇒ wayfinding, not pressure.
> - asked mid-run *"how much noise are you making?"*, they answer roughly right. Fails ⇒ the Ear, or clamor legibility.
> - asked after a death, they explain it in **one sentence** (principle 4). Fails ⇒ combat readability or damage feedback.
> - they discover they can drop loot **without being told**. Fails ⇒ the control list, or the bag.

> **GATE M3 COOP** `pending` — a rank-8 player brings a rank-1 friend into a rank-8 floor (ADR-010). The newcomer is downed repeatedly and *still wants to go again*. If they don't, the ember rescue isn't doing enough work.
>
> **Protocol.** Two machines, remote, not two windows on one desk — `M2-T12` found five faults that only exist over a real connection. One run where a death is allowed to happen rather than staged.
>
> **Preconditions, inherited from `GATE M2 COOP` by ADR-115:**
> - the surviving player notices the ember **without being told it exists**. Fails ⇒ the ember's presentation, not the rescue design.
> - they can say what carrying it costs them — squares, weight, or noise. Fails ⇒ `DES-012`'s sacrifice is invisible, and a sacrifice nobody can feel is not one.
> - the downed player can tell what is happening to them while down. Fails ⇒ the Vörðr readout (`M3-T09`), which is the first build in which this question has an answer at all.

## M4 — Vertical Slice  ·  *largest; art and audio dominate*

> **RESCOPED (ADR-061).** A vertical slice is **a small, polished, fully playable cross-section showing all major systems working together** — typically 10–30 minutes. The previous M4 listed *all six classes* and a 45-minute target, which is **breadth, not a cross-section**, and would have delayed the moment we learn whether the game is good by months of content work.
>
> **M4 ships two classes, not six.** All six remain required *for launch* (ADR-012 — they are available from the start), but they move to **M5**. The slice's job is to prove the game is worth finishing; class breadth is what you build once you know it is.
<!-- milestone id=M4 depends=M3 size=unknown -->
**Goal:** one biome, complete and polished, representative of the final game.
- [ ] `M4-T01` The Delvings: full generation from room modules, 3 floors — *first point at which the Hunt can vary by floor and persist across one (`DES-017`, ADR-037), and at which **the Sealing can lock a Shaft** rather than only make it worse (`DES-005`, ADR-091) — both need a floor beneath the one you are on* → DES-015, DES-006, DES-017, DES-005
- [ ] `M4-T02` ~6 enemy archetypes, 2 hazard types → DES-013
- [ ] `M4-T03` **Two classes**, fully polished — Húskarl and Veiðimaðr, opposite loop relationships. *The other four move to M5 (ADR-061).* → DES-011
- [ ] `M4-T04` Contracts tier 1–3, one faction (`DES-007`) — *the board hangs in the Threshold, which `M2-T06` built as a fire and a doorway* → DES-007, DES-014
- [ ] `M4-T05` Real art pass, real audio, real UI, ping system → ART-001, ART-002, DES-019, DES-012
- [ ] `M4-T06` **Save/load surfaced, settings, controls rebinding** — *narrowed by ADR-116. It read "full save/load", which stopped being true when ADR-109 moved the save system to `M3-T06`: the format, the versioning, the migrations and the write policy are built there and grow a version per `M3` task. What is left here is the part a **player** touches — profile management, and what a build does with a save from a newer build than itself — plus settings and rebinding. Two tasks reading "save/load" is how one of them gets built twice or neither gets built* → TEC-003, DES-018
- [ ] `M4-T11` **The accessibility suite** — colour-blind support with no information in hue alone, UI scaling, dyslexia-friendly font, high contrast, per-bus volume sliders, mono output, and independently adjustable shake / blur / head-bob / FOV. *Split out of `M4-T06` by ADR-077: "settings" was standing in for a dozen deliverables, several of which are architectural constraints rather than options* → DES-018
- [ ] `M4-T07` **Steam networking integration** (lobbies, invites, relay) — before any external playtest → TEC-004
- [ ] `M4-T08` **Ink shader, complete** — hatching (nested triplanar layers), the Threshold/Deep inversion, vertex-colour authoring across the asset library → ART-005
- [ ] `M4-T09` **Composer onboarded; first full stem set** — brief handed over as-is, stems authored to one tempo and key → ART-003
- [ ] `M4-T12` **Audio occlusion and reverb zones** — raycast → lerp `attenuation_filter_cutoff_hz`, and `Area3D` reverb driven by the player's current cell. *Split out by ADR-099: `M2-T03` and `M2-T09` built the adaptive driver, and `TEC-005` was reading as fully implemented because they were its only citers — while the half of it that makes the Deep sound like stone was neither built nor planned. ⟨~a week⟩ by `ART-002`'s own costing, and **portal propagation is for the Gullsjúkr only**, never a general system* → TEC-005, ART-002, DES-015
- [ ] `M4-T13` **The lantern, and darkness as a mechanic** — *`ART-001` is explicit that light is a resource the player manages and that lighting design is gameplay design, and `DES-008` spends a weapon slot on it. No task built it, in any milestone. `M2-T13` lit the floor as far as it can be lit without one: the ambient floor there stays navigable rather than truly dark, because a dark level with no light source is not a mechanic, it is a bug. That ⟨tune⟩ number is the one this task exists to lower* → ART-001, ART-005, DES-008
- [ ] `M4-T10` **Phase 2→3 asset production** — real models replacing blockout, per the schedule and specs → ART-004

> **GATE M4 EXIT** `pending` — shippable-quality **25 minutes** ⟨tune⟩, played solo *and* as a 4-stack, with every major system present and polished. This is what a publisher, a Steam page, or a Kickstarter would see.

> **GATE M4 GREED** `pending` — a playtester **voluntarily abandons loot to survive**, then talks about it afterwards. That's the whole game in one moment.
>
> **Moved here from `M2` by ADR-109**, unchanged in wording, because the sentence was never wrong — only early. It assumes loot and survival are in tension, and that tension is built out of the Tithe (`M3-T04`), which makes a hoard an obligation rather than a score, and classes (`M3-T02`), which make what you brought down something you can regret. Neither existed at `M2`, so the question returned an answer about the blockout.
>
> **Protocol.** Off the back of the slice run above, unprompted. The tester is not asked about loot; if the moment happens they will raise it themselves, and if they have to be asked, it did not happen.
>
> **If it fails with every system present and polished, the pressure model is wrong** — and that conclusion is finally trustworthy, because `GATE M2 EXIT` has already ruled out wayfinding, clamor legibility, combat readability and the bag, and the slice has ruled out "it is grey boxes".

## M5 — Content & Breadth
<!-- milestone id=M5 depends=M4 size=unknown -->
- [ ] `M5-T01` **The remaining four classes** — moved here by ADR-061; all six are required for launch (ADR-012) → DES-011
- [ ] `M5-T02` Remaining biomes — Barrow-Fields, Sunken Wood → DES-006, DES-015
- [ ] `M5-T03` Remaining factions and Aspects → DES-007, DES-004
- [ ] `M5-T04` Full enemy roster and modifier set → DES-013
- [ ] `M5-T05` **Design and build onboarding / the first hour** — `DES-010` C1 is the largest absolute churn point. *Produces a new design doc; deliberately not written yet (ADR-065) — it is not needed to start development, and a reference to a document that does not exist is itself a stub.* → DES-010, PRO-005
- [ ] `M5-T06` Balance passes against real telemetry, at every party size → DES-022

> **GATE M5 EXIT** `pending` — all six classes, three biomes and the full enemy roster complete and balanced; `DES-022`'s balance checks pass at every party size; **self-reported growth across runs 11–25 shows no dip** (the headline metric). Feature-complete: from here, only bugs and polish.

## M6 — Ship
<!-- milestone id=M6 depends=M5 size=unknown -->
Store page, IP attorney review (`PRO-004`), demo, playtesting at scale, launch.

---

## Standing risks

| Risk | Severity | Mitigation |
|---|---|---|
| Meta-progression trivializes runs | **High** | Tithe counter-pressure (`DES-003`); guardrail tests at M3 |
| Scope creep via biomes/enemies | **High** | Hard caps in `DES-001`; systems over content |
| Procedural levels feel samey | Medium | Hand-authored modules, not generated geometry (`TEC-001`) |
| Save migration retrofit | Medium | Versioning from commit one (`TEC-003`) |
| Hunt is annoying rather than tense | Medium | Counter-play tools first, not last; heavy M2 playtesting |
| Solo-dev burnout | **High** | Milestone gates with real exit criteria; ship the slice before breadth |
| IP exposure | Low (if `PRO-004` followed) | Guardrails + pre-launch legal review |
| **Godot high-level multiplayer doesn't scale to our object counts** | **High** | M1 spike test is an explicit go/no-go (`TEC-004`) |
| **Co-op roughly doubles QA surface** | **High** | Automated 2-client smoke tests in CI from M1; accept slower milestones rather than skipping the tests |
| Class balance across 6 classes × 5 Aspects | Medium | Shared tree + small Rite branches (ADR-009) keeps the surface small; every class solo-viable is a hard rule |

## Estimates are lies

The ⟨week⟩ figures assume roughly one focused full-time developer and are **optimistic by the usual factor**. Treat ordering as the real content of this document; treat durations as relative weights, not commitments.
